version: '3.8'
services:

  sse:
    container_name: sse
    build:
      context: ..
      dockerfile: docker/Dockerfile_with_build
    ports:
      - "8090:8080"
    environment:
      SPRING_ACTIVE_PROFILE: local
    networks:
      - app-tier
    restart: always

networks:
  app-tier:
    external: true
    name: app-tier
